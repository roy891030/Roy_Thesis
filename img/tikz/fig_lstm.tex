\documentclass[tikz,border=14pt]{standalone}
\usepackage[no-math]{fontspec}
\usepackage{xeCJK}
\usepackage[UTF8,fontset=none]{ctex}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,calc,backgrounds}

\IfFontExistsTF{Times New Roman}{\setmainfont{Times New Roman}}{\setmainfont{TeX Gyre Termes}}
\IfFontExistsTF{Kaiti TC}{\setCJKmainfont[AutoFakeBold=3]{Kaiti TC}}
  {\IfFontExistsTF{Songti TC}{\setCJKmainfont[AutoFakeBold=3]{Songti TC}}
    {\setCJKmainfont{PingFang TC}}}
\input{thesis_colors}

\begin{document}
\begin{tikzpicture}[
  % ── cell-state operation squares ──────────────────────────
  cellop/.style={draw=thesisDark, fill=thesisDark!14,
                 rectangle, minimum size=13mm, thick, inner sep=0pt,
                 font=\Large\bfseries},
  % ── small intermediate ×  ────────────────────────────────
  smallop/.style={draw=thesisDark, fill=thesisDark!14,
                  rectangle, minimum size=10mm, thick, inner sep=0pt,
                  font=\large\bfseries},
  % ── gate circles (3 colours) ─────────────────────────────
  fgate/.style={circle, draw=thesisBlue, fill=thesisBlue!18,
                very thick, minimum size=22mm, font=\LARGE\bfseries},
  ugate/.style={circle, draw=thesisForest, fill=thesisForest!18,
                very thick, minimum size=22mm, font=\LARGE\bfseries},
  ogate/.style={circle, draw=thesisCrimson, fill=thesisCrimson!14,
                very thick, minimum size=22mm, font=\LARGE\bfseries},
  % ── arrows / wires ───────────────────────────────────────
  arr/.style={-{Stealth[length=4mm,width=3.5mm]}, very thick, thesisDark},
  wire/.style={very thick, thesisDark},
  jdot/.style={fill=thesisDark, circle, minimum size=7pt, inner sep=0pt},
  % ── labels ───────────────────────────────────────────────
  iolabel/.style={font=\Large, thesisDark},
  seclabel/.style={font=\large\bfseries},
  eqlabel/.style={font=\normalsize\itshape, thesisDark!65},
]

%% ─────────────────────────────────────────────────────────
%% COORDINATE GRID
%%   y=7.0  cell state highway
%%   y=5.2  g×i intermediate / output path
%%   y=3.0  gate circles
%%   y=1.2  h_{t-1} input wire
%%   y=0.0  x_t label (below)
%%
%%   x=3.0  ×_f  /  f gate
%%   x=8.5  +_u  /  ×_gi at y=5.2  /  g(x=7.0) and i(x=10.0)
%%   x=13.0 junction •
%%   x=14.5 ×_o  /  o gate
%% ─────────────────────────────────────────────────────────

%% ── 1. CELL STATE HIGHWAY ────────────────────────────────
\draw[wire] (-0.4, 7.0) node[left, iolabel]{$c_{t-1}$} -- (2.35, 7.0);
\node[cellop] (Xf)  at (3.0,  7.0) {$\times$};
\draw[wire]   (Xf.east) -- (7.95, 7.0);
\node[cellop] (Pu)  at (8.5,  7.0) {$+$};
\draw[wire]   (Pu.east) -- (12.7, 7.0);
\node[jdot]   (Jct) at (13.0, 7.0) {};
\draw[wire]   (13.0,7.0) -- (16.3, 7.0) node[right, iolabel]{$c_t$};

%% ── 2. OUTPUT PATH (junction → L-bend → ×_o → h_t) ─────
% Vertical drop from junction
\draw[wire] (13.0, 7.0) -- (13.0, 5.2);
% Horizontal run to ×_o
\draw[wire] (13.0, 5.2) -- (13.95, 5.2);
\node[cellop] (Xo) at (14.5, 5.2) {$\times$};
% h_t exit
\draw[arr]  (Xo.east) -- (16.3, 5.2) node[right, iolabel]{$h_t$};
% tanh label on the output bend
\node[font=\normalsize\itshape, thesisDark!70, above=3pt] at (13.0, 5.6) {tanh};

%% ── 3. g×i INTERMEDIATE MULTIPLY (below +_u) ────────────
\node[smallop] (Xgi) at (8.5, 5.2) {$\times$};

%% ── 4. GATE CIRCLES ──────────────────────────────────────
\node[fgate] (f) at (3.0,  3.0) {$f_t$};
\node[ugate] (g) at (7.0,  3.0) {$\tilde{c}_t$};
\node[ugate] (i) at (10.0, 3.0) {$i_t$};
\node[ogate] (o) at (14.5, 3.0) {$o_t$};

%% ── 5. GATE → CELL STATE CONNECTIONS ────────────────────
% f → ×_f  (vertical arrow, perfectly aligned)
\draw[arr] (f.north) -- (Xf.south);

% g and i → ×_gi  (symmetric diagonals; TikZ clips at Xgi border)
\draw[arr] (g.north) -- (Xgi);
\draw[arr] (i.north) -- (Xgi);

% ×_gi → +_u  (vertical arrow)
\draw[arr] (Xgi.north) -- (Pu.south);

% o → ×_o  (vertical arrow, perfectly aligned)
\draw[arr] (o.north) -- (Xo.south);

%% ── 6. INPUT WIRE (h_{t-1}) AND x_t ─────────────────────
% Horizontal h_{t-1} wire
\draw[wire] (-0.4, 1.2) node[left, iolabel]{$h_{t-1}$} -- (15.5, 1.2);

% x_t enters vertically at x=5.5
\draw[wire] (5.5, -0.6) node[below, iolabel]{$x_t$} -- (5.5, 1.2);
\node[jdot] at (5.5, 1.2) {};

% Branch points on h_{t-1} wire going up to each gate
\foreach \gx/\gnode in {3.0/f, 7.0/g, 10.0/i, 14.5/o}{
  \node[jdot] at (\gx, 1.2) {};
  \draw[arr] (\gx, 1.2) -- (\gx, {3.0 - 1.1});
}

%% ── 7. SECTION BACKGROUNDS & LABELS ─────────────────────
\begin{pgfonlayer}{background}
  \fill[thesisBlue!8]    (1.4, -0.9) rectangle (5.5, 8.2);
  \fill[thesisForest!8]  (5.5, -0.9) rectangle (11.5, 8.2);
  \fill[thesisCrimson!8] (11.5, -0.9) rectangle (16.5, 8.2);
  \draw[thesisDark!35, thick, rounded corners=4pt]
       (1.4, -0.9) rectangle (16.0, 8.2);
\end{pgfonlayer}

\node[seclabel, thesisBlue!80!thesisDark]
      at (3.0, 8.85) {Forget};
\node[seclabel, thesisForest!80!thesisDark]
      at (8.5, 8.85) {Update};
\node[seclabel, thesisCrimson!80!thesisDark]
      at (14.0, 8.85) {Output};

%% ── 8. LEGEND BOX ────────────────────────────────────────
\node[draw=thesisDark!40, rounded corners=4pt, fill=white,
      inner sep=10pt, font=\normalsize, align=left, anchor=north west]
  at (17.2, 8.8)
  {$x_t$\,：輸入特徵\\[3pt]
   $h_t$\,：隱藏狀態\\[3pt]
   $c_t$\,：記憶狀態\\[3pt]
   $f_t$\,：遺忘閘（$\sigma$）\\[3pt]
   $\tilde{c}_t$\,：候選記憶（tanh）\\[3pt]
   $i_t$\,：輸入閘（$\sigma$）\\[3pt]
   $o_t$\,：輸出閘（$\sigma$）};

\end{tikzpicture}
\end{document}
