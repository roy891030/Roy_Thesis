\documentclass[tikz,border=14pt]{standalone}
\usepackage[no-math]{fontspec}
\usepackage{xeCJK}
\usepackage[UTF8,fontset=none]{ctex}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,fit,calc}

\IfFontExistsTF{Times New Roman}{\setmainfont{Times New Roman}}{\setmainfont{TeX Gyre Termes}}
\IfFontExistsTF{Kaiti TC}{\setCJKmainfont[AutoFakeBold=3]{Kaiti TC}}
  {\IfFontExistsTF{Songti TC}{\setCJKmainfont[AutoFakeBold=3]{Songti TC}}
    {\setCJKmainfont{PingFang TC}}}
\input{thesis_colors}

\begin{document}
\begin{tikzpicture}[
  gnode/.style={circle, draw=thesisDark, fill=thesisTeal!30, very thick,
                minimum size=14mm, font=\normalsize\bfseries},
  arr/.style={-{Stealth[length=2.8mm,width=2.2mm]}, thick, thesisDark},
  % ── table cell styles ────────────────────────────────────
  hcell/.style={draw=thesisDark, fill=thesisLight, minimum width=18mm,
                minimum height=9mm, inner sep=3pt, font=\small\bfseries, align=center},
  lcell/.style={draw=thesisDark, fill=thesisLight!60, minimum width=18mm,
                minimum height=9mm, inner sep=3pt, font=\small, align=center},
  dcell/.style={draw=thesisDark, fill=white, minimum width=12mm,
                minimum height=9mm, inner sep=3pt, font=\small\ttfamily, align=center},
]

%%═════════════════════════════════════════════════════════
%% LEFT: 3-node graph
%%═════════════════════════════════════════════════════════
\node[gnode] (A) at (0,  2.4) {A};
\node[gnode] (B) at (-1.6, 0) {B};
\node[gnode] (C) at ( 1.6, 0) {C};

% Self-loop on A (top)
\draw[arr, looseness=7.5, in=160, out=20] (A.north) to (A.north);

% Bidirectional edges: slight curve so both directions are visible
\draw[arr, bend left=18] (A) to (B);
\draw[arr, bend left=18] (B) to (A);
\draw[arr, bend left=18] (A) to (C);
\draw[arr, bend left=18] (C) to (A);
\draw[arr, bend left=18] (B) to (C);
\draw[arr, bend left=18] (C) to (B);

% Bounding box with label
\node[draw=thesisDark!55, dashed, rounded corners=5pt, inner sep=14pt,
      fit=(A)(B)(C), label={[font=\small, thesisDark]above:示意圖（小型 Graph）}]
     (graphbox) {};

%%═════════════════════════════════════════════════════════
%% RIGHT: edge_index table
%%═════════════════════════════════════════════════════════
\begin{scope}[xshift=6.2cm, yshift=0.3cm]

  % Header row — spans all 6 data columns + label column
  \node[hcell, minimum width=97mm] (hdr) at (3.82, 1.8) {edge\_index};

  % Row label: start
  \node[lcell] (lstart) at (0, 0.9) {start};
  % Row label: end
  \node[lcell] (lend)   at (0, 0.0) {end};

  % Data cells — start row: A A B B C C
  \foreach \col/\v in {1/A, 2/A, 3/B, 4/B, 5/C, 6/C}{
    \node[dcell] at ({1.22*\col + 0.61}, 0.9) {\v};
  }
  % Data cells — end row:  B C A C A B
  \foreach \col/\v in {1/B, 2/C, 3/A, 4/C, 5/A, 6/B}{
    \node[dcell] at ({1.22*\col + 0.61}, 0.0) {\v};
  }

\end{scope}

\end{tikzpicture}
\end{document}
