\chapter{實驗結果}
\label{ch:results}

本章根據正式實驗結果，系統性地回答本文的核心研究問題：在台灣股票市場橫斷面報酬預測任務中，模型結構、圖拓樸設計與訓練視窗長度如何共同影響截面排序訊號品質與投資組合表現。完整實驗共 27 組，涵蓋短期、中期、長期三種訓練視窗，以及線性迴歸、XGBoost、LSTM、DNN、GAT-industry、GAT-universe、GAT-TwoGraph、GAT-IndNeutral 與 GAT-full 九類模型。非圖基準模型不使用圖結構；GAT 拓樸系列與 GAT 中性化系列均採用相同封存資料與圖結構，以確保比較結果不受資料切分差異或圖重估方式干擾。

本章以 Daily IC 與 ICIR 作為主要評估指標，因本研究任務為橫斷面排序而非單純數值迴歸，排序訊號的跨期穩健性比均方誤差更直接反映模型在截面投資策略中的可用性。投資組合績效則用以驗證排序訊號是否能轉化為前 10\% 等權做多策略的實質報酬。各節依序以非圖基準模型、圖拓樸比較與中性化機制為主軸展開分析，最後以訓練視窗長度的橫向比較統整各類模型的跨期表現。

% =========================================================
\section{實驗概述與市場背景}
\label{sec:overview}
% =========================================================

本章所有結果均在統一實驗框架下計算，各訓練視窗的測試期對應不同市場環境，解讀時需一併考慮背景差異。表 \ref{tab:results_benchmark_0050} 列出三個測試視窗的台灣 50 ETF（0050）基準績效。短期視窗測試期為 2024-12-03 至 2025-05-29，0050 年化報酬為 -5.12\%、Sharpe 比率為 -0.150，最大跌幅達 -27.37\%；中期視窗測試期對應 0050 年化報酬 25.52\%，長期視窗測試期則為 24.12\%。三個測試視窗分別覆蓋下行、溫和上行與持續多頭等不同市場情境，因此不宜直接跨視窗比較絕對報酬數字，亦不宜以短期視窗結果外推長期結論。

\input{Sections/tables/results_benchmark_0050.tex}

表~\ref{tab:results_benchmark_0050} 顯示，短期視窗測試期之 0050 年化報酬與 Sharpe 比率皆為負值，中期與長期視窗則為正值。後續各模型投資組合績效之解讀，均需與各自測試期之市場背景合併考量。

\FloatBarrier

表~\ref{tab:results_overview_master} 彙整全部 27 組實驗的訓練集與測試集核心指標，供讀者在進入各節細部討論前掌握完整數值全貌。各視窗最佳測試 Daily IC、測試 ICIR 與測試 Sharpe 以粗體標示；Daily IC 測試/訓練比值反映各模型的樣本外泛化能力。

\input{Sections/tables/results_overview_master.tex}

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{img/ch4_fig_portfolio_metrics_overview.png}
  \caption{各模型類別於三個訓練視窗所對應測試集之 Daily IC、ICIR、Sharpe 比率與總報酬走勢（GAT 拓樸系列：GAT-industry、GAT-universe、GAT-TwoGraph；GAT 中性化系列：GAT-IndNeutral、GAT-full）。}
  \label{fig:ch4_portfolio_metrics_overview}
\end{figure}

圖~\ref{fig:ch4_portfolio_metrics_overview} 以與表~\ref{tab:results_overview_master} 相同之數值呈現各模型類別跨訓練視窗的變化。圖中可見短期視窗之 Sharpe 比率與總報酬多為負值，中期與長期視窗則整體改善；GAT 中性化系列於中期與長期視窗的 Daily IC 與 ICIR 表現較高。

由表~\ref{tab:results_overview_master} 與圖~\ref{fig:ch4_portfolio_metrics_overview} 可歸納四項全局規律，作為後續各節分析的敘事框架：

\begin{enumerate}
  \item \textbf{高報酬不等於高訊號品質：訓練/測試比值是必要診斷。}XGBoost 長期總報酬達 143\%，但 Daily IC 測試/訓練比值僅 0.16，為全模型過擬合程度最嚴重者（詳見第~\ref{subsec:xgboost_results}~節）。表中「Daily IC 測/訓比」欄位提供各模型泛化能力的快速診斷，並應與 ICIR 及 Sharpe 指標共同解讀。DNN 的測試 ICIR（0.726）在長期視窗為非圖模型上界，是評估圖結構增量貢獻的合理基準。

  \item \textbf{圖拓樸設計是必要非充分條件。}GAT-universe（全市場圖）在長期視窗的 ICIR 僅 0.526，低於 GAT-industry（0.677）與 DNN（0.726），說明圖若不搭配具有經濟意義的拓樸，將因過度平滑化降低截面辨識力。統計檢定進一步確認：僅中性化延伸模型（GAT-IndNeutral、GAT-full）能在 Diebold-Mariano 配對檢定中顯著優於 DNN（詳見第~\ref{subsec:graph_contribution}~節）。

  \item \textbf{GAT 中性化系列在長期視窗多指標同時領先。}長期視窗 GAT-IndNeutral 取得最高 ICIR（0.786），GAT-full 取得最高 Sharpe 比率（1.849）；兩者的優勢隨訓練資料充足而持續擴大。

  \item \textbf{訓練視窗長度是可投資性的前提閾值。}短期視窗（116 天）下九個模型的 Sharpe 比率介於 $-0.423$ 至 $+0.057$，測試總報酬均為負；中、長期視窗 Sharpe 整體轉正，長期最佳達 1.849。訓練資料規模決定排序訊號能否轉化為可投資報酬，優先於架構設計本身。
\end{enumerate}

\FloatBarrier

% =========================================================
\section{非圖基準模型之實證分析}
\label{sec:baseline_results}
% =========================================================

本節依序分析線性迴歸、XGBoost、LSTM 與 DNN 四類非圖基準模型。此四類模型均不使用圖結構，其結果一方面作為後續圖注意力模型的比較基準，另一方面直接揭示特徵工程在截面排序任務中的有效程度。

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{img/ch4_fig_baseline_comparison.png}
  \caption{非圖基準模型於不同訓練視窗所對應測試集之 Daily IC（左）與 Sharpe 比率（右）。黃色為短期、綠色為中期、藍色為長期訓練視窗。}
  \label{fig:ch4_baseline_comparison}
\end{figure}

圖~\ref{fig:ch4_baseline_comparison} 顯示四類非圖基準模型在三個訓練視窗下的測試 Daily IC 與 Sharpe 比率。短期視窗中，僅 XGBoost 的 Sharpe 比率略高於零；中期與長期視窗中，四類模型的 Sharpe 比率均轉為正值。DNN 的 Daily IC 在短期與長期視窗中為非圖基準模型最高，中期視窗則以 XGBoost 最高。

\subsection{線性迴歸：線性擬合的侷限}
\label{subsec:linear_results}

線性迴歸在本文所有模型中整體表現最弱。三個訓練視窗的平均 Daily IC 僅 0.0168，短期視窗 Sharpe 比率為 -0.423，顯示 56 維技術與估值特徵之間存在難以透過線性擬合捕捉的非線性交互作用。即便在訓練樣本最充足的長期視窗下，線性迴歸的截面排序能力仍明顯落後於其他非圖模型。此結果確認橫斷面報酬預測任務的非線性本質，印證以非線性模型作為主要實驗架構的必要性。

\input{Sections/tables/results_linear_detail.tex}

表~\ref{tab:results_linear_detail} 顯示，Linear 的測試 Daily IC 於中期與長期視窗均高於短期視窗，Sharpe 比率亦由短期視窗的負值轉為中期與長期視窗的正值；然而，其測試 Daily IC 與測試 ICIR 仍低於其他非圖基準模型。

\subsection{XGBoost：梯度提升模型之過擬合診斷}
\label{subsec:xgboost_results}

XGBoost 在長期視窗的投資組合總報酬達 143.08\%，為本文所有模型中最高。然而，表 \ref{tab:results_xgboost_gap} 顯示，其長期視窗訓練 Daily IC 為 0.2570，測試 Daily IC 僅為 0.0408，Daily IC 測試/訓練比為 0.16，顯示在投資組合層級的高報酬背後隱含顯著的過擬合風險。

\input{Sections/tables/results_xgboost_gap.tex}

表~\ref{tab:results_xgboost_gap} 亦顯示，XGBoost 在三個訓練視窗的 Daily IC 測試/訓練比均偏低，其中短期視窗僅為 0.06，長期視窗亦僅為 0.16。本文將 XGBoost 定位為梯度提升模型的代表性基準，但其訊號穩健性需一併揭露，不宜以單一總報酬指標作為模型品質的最終評判依據。

\subsection{LSTM：時間序列記憶的邊際效益}
\label{subsec:lstm_results}

LSTM 在長期視窗的 Sharpe 比率達 1.336，較線性迴歸有所改善，顯示個股時間序列的歷史記憶機制具有一定程度的排序效益。然而，LSTM 在三個視窗的 Daily IC 與 Sharpe 比率均低於 DNN、GAT 拓樸系列與 GAT 中性化系列，顯示以逐股時序方式建模在截面排序任務中的邊際貢獻有限。

\input{Sections/tables/results_lstm_detail.tex}

表~\ref{tab:results_lstm_detail} 顯示，LSTM 的測試 Daily IC、測試 ICIR、測試 Sharpe 與測試總報酬均隨訓練視窗延長而改善；其中長期視窗測試 Daily IC 為 0.0418，Sharpe 比率為 1.336。

截面排序的核心在於比較同一時點不同股票的相對強弱，而 LSTM 以逐股時序方式獨立建模，無法直接利用截面內各股票之間的關係資訊。相對地，DNN 透過跨股截面批次正規化與全連接層，較 LSTM 更適合從多股共同特徵中萃取截面排序訊號，此點亦在下一小節得到驗證。

\subsection{DNN：非圖模型的截面排序效益}
\label{subsec:dnn_results}

DNN 是本文非圖基準模型中最具參考價值的對照組。儘管不使用任何圖結構，DNN 在短期視窗取得全模型最高 Daily IC（0.0396）與 ICIR（0.408）；在中期視窗取得最高 Sharpe 比率（0.980）與最高總報酬（39.13\%）；在長期視窗亦達 Daily IC 0.0535 與 Sharpe 比率 1.564，與 GAT 拓樸系列及 GAT 中性化系列具有競爭性。

\input{Sections/tables/results_dnn_detail.tex}

表~\ref{tab:results_dnn_detail} 顯示，DNN 的測試 Daily IC 與測試 ICIR 由短期至長期視窗持續提高；短期視窗雖具有較高的測試 Daily IC，測試 Sharpe 與測試總報酬仍為負值，中期與長期視窗則轉為正值。

此結果具有重要的方法論意義：56 維技術與估值特徵搭配截面標準化的特徵工程設計，已在本資料集中提供相當程度的截面排序能力。後續第 \ref{sec:topology_results} 節與第 \ref{sec:neutralization_results} 節所討論的圖注意力機制效益，均應以 DNN 作為非圖對照基準進行評估，而非以更弱的線性或 LSTM 基準作為比較起點。

\FloatBarrier

% =========================================================
\section{圖拓樸結構之排序效益比較}
\label{sec:topology_results}
% =========================================================

本節在建立非圖基準後，進一步比較不同圖拓樸設計的截面排序效益。分析以長期視窗為主要討論情境，因長期視窗的訓練樣本最充足，能較清楚地區分圖拓樸設計的差異效果；中期視窗結果作為輔助參照。

\subsection{產業圖與全市場圖之對比}
\label{subsec:industry_vs_universe}

表 \ref{tab:results_long_topology_train_test} 聚焦比較 DNN、GAT-industry 與 GAT-universe。訓練 Daily IC 反映樣本內擬合能力，測試 Daily IC 與測試 ICIR 則直接衡量樣本外排序訊號品質。最直接的觀察是，GAT-universe 的測試 Daily IC（0.0452）與測試 Sharpe（1.506）均低於不使用圖結構的 DNN（測試 Daily IC 0.0535，測試 Sharpe 1.564）。這表示全截面完全連結並不必然提供有效的增量資訊：當每一檔股票都與全市場所有股票交換訊息時，模型容易過度吸收市場共同成分，反而削弱個股截面差異的辨識能力，即文獻所指之過度平滑化效應。

\input{Sections/tables/results_long_topology_train_test.tex}

相對地，GAT-industry 在長期視窗的 Daily IC 為 0.0543、Sharpe 比率為 1.779，均優於 GAT-universe 與 DNN。中期視窗的結果方向一致：GAT-industry 的 Daily IC（0.0445）與 Sharpe 比率（0.900）均高於 GAT-universe（0.0377、0.755）。此結果支持具有經濟意義之局部圖拓樸的有效性：產業圖提供相對穩定的同業關係連結，使模型能在同產業股票之間進行相對強弱比較，而不引入與個股截面差異無關的全市場共同波動。

\subsection{雙圖架構（GAT-TwoGraph）之效益分析}
\label{subsec:twograph_results}

GAT-TwoGraph 同時融合產業圖與全市場圖的訊息傳遞，其結果呈現截面排序訊號品質與投資組合風險報酬之間的取捨。長期視窗下，GAT-TwoGraph 的 Daily IC 為 0.0512，低於 GAT-industry 的 0.0543，但 Sharpe 比率提升至 1.827。

\input{Sections/tables/results_twograph_detail.tex}

表~\ref{tab:results_twograph_detail} 顯示，中期視窗下 GAT-TwoGraph 的測試 Daily IC 與測試 ICIR 均高於 GAT-industry，但測試 Sharpe 與測試總報酬較低；長期視窗下 GAT-TwoGraph 的測試 Sharpe 與測試總報酬較高，但測試 Daily IC 與測試 ICIR 較低。此結果說明，雙圖架構在長期視窗下可改善投資組合的風險報酬特徵，但平均截面排序訊號品質未必同步提升。圖拓樸的選擇應視評估目標而定，以 ICIR 衡量排序訊號穩健性或以 Sharpe 比率衡量投資組合效益，兩者的最佳拓樸選擇未必相同。

\FloatBarrier

% =========================================================
\section{中性化機制分析}
\label{sec:neutralization_results}
% =========================================================

本節分析 GAT-IndNeutral 與 GAT-full 兩種加入中性化與因子注意力機制的延伸模型，並探討注意力特徵在不同訓練視窗下的分布規律。

\subsection{GAT-IndNeutral 與 GAT-full 之比較}
\label{subsec:indneutral_vs_full}

GAT 中性化系列在中期與長期視窗的 ICIR 表現最為突出。中期視窗中，GAT-full 取得全模型最高 Daily IC（0.0596）與 ICIR（0.628）；長期視窗中，GAT-IndNeutral 取得最高 Daily IC（0.0590）與 ICIR（0.786），GAT-full 亦有 Daily IC 0.0583 與 Sharpe 比率 1.849。以 ICIR 作為衡量排序訊號跨期穩定性的指標，GAT 中性化系列在長期視窗的表現已明顯優於 GAT 拓樸系列，亦優於不使用圖結構的 DNN。

為驗證上述 IC 優勢的統計顯著性，針對長期視窗 GAT-IndNeutral 的逐日 IC 序列進行正式統計檢定。

\textbf{Newey-West HAC $t$ 檢定。}截面 IC 序列並非獨立同分布（i.i.d.）。由於同一批股票在相鄰交易日之間共享部分資訊環境，IC 序列存在顯著的序列自相關（長期視窗一階自相關約 0.53），若以普通最小平方法的 naive 標準誤計算 $t$ 統計量，將因忽略自相關而系統性高估顯著性。Newey-West 異質性-自相關一致（HAC）估計量~\cite{newey1987simple}在計算標準誤時納入 $L$ 個滯後期的自協方差加權修正，本文依 Andrews（1991）~\cite{andrews1991autocovariance}自動選階規則設定 lag $= 5$，相當於覆蓋約一週的交易日序列依存結構。修正後，GAT-IndNeutral 的 NW $t = 11.76$（$p < 0.001$），DNN 為 $t = 10.83$（$p < 0.001$）；兩者均在高度顯著水準下拒絕「平均 IC 為零」的虛無假說，確認長期視窗的截面排序訊號具有穩健的統計意義，而非僅因樣本期內少數極端月份所致。

\textbf{Block Bootstrap ICIR 信賴區間。}ICIR 為 IC 均值除以 IC 標準差，其抽樣分布在有序列相關的資料中不服從標準常態，需以重抽樣法估計。採用環形 Block Bootstrap（block size $= 10$，重複 $B = 2000$ 次），在每次重抽樣中保留序列的局部時間依存結構，並計算 95\% 百分位信賴區間。結果：GAT-IndNeutral 之 ICIR 95\% CI 為 $[0.624,\ 0.957]$，下界遠大於零，表明排序訊號的跨期一致性（而非單純均值）本身即達顯著水準；DNN 之 CI 為 $[0.563,\ 0.901]$，同樣顯著但區間下界略低、寬度略寬，對應於其逐日 IC 方差相對較大的特性。

\textbf{月度勝率。}從月度維度觀察，長期視窗測試期共 28 個月中，GAT-IndNeutral 有 27 個月 IC 為正（月度勝率 96.4\%，月度 ICIR $= 1.866$）；DNN 則達到 28/28 月（100\%），但月度 ICIR 為 1.763，低於 GAT-IndNeutral。此結果說明 GAT-IndNeutral 雖偶有單月小幅負 IC，但其平均逐日 IC 的跨月穩定性更高，兩者互為補充：DNN 的勝率更穩但每日訊號波動較大，GAT-IndNeutral 的平均訊號強度更高。

\input{Sections/tables/results_stat_tests.tex}

表~\ref{tab:results_stat_tests} 彙整長期視窗之統計穩健性檢定結果。Newey-West HAC $t$ 檢定與 Block Bootstrap ICIR 信賴區間顯示 GAT-IndNeutral 與 DNN 的排序訊號皆具有統計意義；Diebold-Mariano 配對比較則顯示 GAT-IndNeutral 與 GAT-full 對 DNN 的差異達顯著水準。

表 \ref{tab:results_dmfm_tradeoff} 進一步呈現 GAT 拓樸系列與 GAT 中性化系列於中期及長期視窗的差異。中期視窗中，GAT-full 取得最高測試 Daily IC（0.0596）與測試 ICIR（0.628）；長期視窗中，GAT-IndNeutral 取得最高測試 Daily IC（0.0590）與測試 ICIR（0.786），GAT-full 則取得最高測試 Sharpe（1.849）與測試總報酬（139.08\%）。

\input{Sections/tables/results_dmfm_tradeoff.tex}

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{img/ch4_fig_neutralization.png}
  \caption{GAT 拓樸系列與 GAT 中性化系列於中期及長期訓練視窗之 Daily IC、ICIR 與 Sharpe 比率比較。綠色為中期、藍色為長期訓練視窗。}
  \label{fig:ch4_neutralization}
\end{figure}

圖~\ref{fig:ch4_neutralization} 顯示，GAT 中性化系列於中期與長期視窗的 Daily IC 與 ICIR 整體較高；長期視窗中，GAT-full 的 Sharpe 比率最高，GAT-IndNeutral 的 Daily IC 與 ICIR 則最高。此結果呈現排序訊號穩定性與投資組合績效之間的差異。

\subsection{因子注意力特徵之描述性分析}
\label{subsec:attention_analysis}

表 \ref{tab:results_dmfm_attention} 顯示各訓練視窗下 GAT-full 因子注意力機制的高頻特徵。短期視窗較常出現 \texttt{down\_vol\_20}、\allowbreak \texttt{roll\_max\_10}、\allowbreak \texttt{roll\_min\_60}、\allowbreak \texttt{roll\_max\_5} 與 \texttt{pct\_to\_low\_20}，集中於近期價格位置、下行波動與短期動能訊號；中期視窗以 \texttt{atr\_14}、\allowbreak \texttt{roll\_min\_5}、\linebreak[4] \texttt{roll\_max\_20}、\allowbreak \texttt{macd} 與 \texttt{std\_ret\_20} 為主；長期視窗中，\texttt{ps}（股價銷售比）與 \texttt{macd\_signal} 進入高頻特徵，並搭配 rolling min/max 與 \texttt{atr\_14}。

\input{Sections/tables/results_dmfm_attention.tex}

表~\ref{tab:results_dmfm_attention} 顯示，不同訓練視窗的高頻注意力特徵並不相同。上述模式與金融直覺一致：短期視窗的訓練樣本有限，模型傾向依賴近期價格位置與波動率等即時性較高的技術訊號；長期視窗的訓練期橫跨多個市場情境，模型較能穩定吸收估值特徵（如 \texttt{ps}）與中長期動能指標。需注意的是，注意力權重僅作為描述性解釋，反映模型在輸入特徵上的加權偏好，不應直接視為特徵對報酬的因果效果。

\FloatBarrier

% =========================================================
\section{訓練視窗長度之跨模型效應}
\label{sec:window_results}
% =========================================================

前三節以模型結構與圖拓樸為主軸，分別分析非圖基準、圖拓樸選擇與中性化機制的截面排序效益。本節以訓練視窗長度為橫向維度，就第~\ref{sec:overview}~節表~\ref{tab:results_overview_master} 所呈現的全局數值，進一步展開逐視窗的細部分析。

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{img/ch4_fig_window_effect.png}
  \caption{各模型類別隨訓練視窗長度之 Sharpe 比率（左）與 Daily IC（右）趨勢。}
  \label{fig:ch4_window_effect}
\end{figure}

圖~\ref{fig:ch4_window_effect} 顯示，各模型類別之測試 Sharpe 比率與測試 Daily IC 整體隨訓練視窗延長而提高，其中 GAT 中性化系列於中期與長期視窗的測試 Daily IC 較高。

\input{Sections/tables/results_static_long_topology.tex}

表~\ref{tab:results_static_long_topology} 為表~\ref{tab:results_overview_master} 的長期視窗明細，呈現九類模型於相同長期訓練視窗下的核心指標。長期視窗中，GAT-IndNeutral 的測試 Daily IC 與測試 ICIR 最高，GAT-full 的測試 Sharpe 最高，XGBoost 的測試總報酬最高。

\input{Sections/tables/results_portfolio_metrics.tex}

表~\ref{tab:results_portfolio_metrics} 以模型類別為橫向切面，呈現各訓練視窗所對應測試集的平均指標。短期視窗的訓練樣本最為有限，多數模型雖已具正向 Daily IC，但測試總報酬均為負。DNN 在短期視窗的 Daily IC 為 0.0396，Sharpe 比率卻為 $-0.098$；GAT 中性化系列的平均 Daily IC 為 0.0278，平均 Sharpe 比率僅 $-0.084$。短期視窗下弱正向的排序訊號，尚不足以在扣除交易成本前穩定轉化為可投資報酬。

中期視窗擴大訓練樣本後，各模型表現明顯改善。GAT 中性化系列的平均 Daily IC 提升至 0.0548，平均 Sharpe 比率提升至 0.873；DNN 亦達 Sharpe 比率 0.980。長期視窗的改善最為穩定：GAT 中性化系列平均 Daily IC 為 0.0587、平均 ICIR 為 0.760、平均 Sharpe 比率為 1.826；GAT 拓樸系列平均 Daily IC 為 0.0502、平均 Sharpe 比率為 1.704；DNN 為 Daily IC 0.0535、Sharpe 比率 1.564。長期視窗的訓練期橫跨多種市場情境，使模型所學習的截面排序關係在樣本外更具泛化能力。

短期視窗中，DNN 在 Daily IC（0.0396）與 ICIR（0.408）上最高，但最佳 Sharpe 比率僅 GAT-industry 的 0.057，且所有模型總報酬均為負；中期視窗中，GAT-full 取得最高 Daily IC（0.0596）與 ICIR（0.628），DNN 取得最高 Sharpe 比率（0.980）與總報酬（39.13\%）；長期視窗中，GAT-IndNeutral 取得最高 Daily IC（0.0590）與 ICIR（0.786），GAT-full 取得最高 Sharpe 比率（1.849），XGBoost 取得最高總報酬（143.08\%，但伴隨顯著過擬合風險，詳見第 \ref{subsec:xgboost_results} 節）。

此結果揭示一項方法論上的重要考量：若僅以短期視窗進行模型比較，易低估圖注意力模型與 GAT 中性化系列的潛在效益；若僅觀察長期視窗的投資組合報酬，則因 XGBoost 的過擬合情形高估其排序穩健性。較完整的評估方式應同時報告三個訓練視窗，並以 IC/ICIR 與投資組合績效共同衡量。

\FloatBarrier

% =========================================================
\section{綜合詮釋與研究限制}
\label{sec:discussion}
% =========================================================

\subsection{評估指標選擇之方法論考量}
\label{subsec:metric_discussion}

本實驗顯示，均方根誤差（RMSE）不適合作為跨架構的唯一比較指標。以長期視窗為例，線性迴歸、LSTM 與 GAT-industry 的 RMSE 約為 0.0484 至 0.0485，而 DNN、GAT-IndNeutral 與 GAT-full 的 RMSE 分別為 0.1336、0.1160 與 0.1817；然而後三者在 Daily IC、ICIR 與 Sharpe 比率上均明顯優於前者。RMSE 偏高的原因在於因子注意力與中性化模型的輸出尺度與原始報酬空間不完全一致，且截面排序任務所需者為相對強弱排序而非精確數值預測。此結果印證以 IC/ICIR 為主要評估指標的方法論選擇：排序訊號的跨期穩健性比均方誤差更直接反映模型在橫斷面投資策略中的可用性。

\subsection{圖模型增量貢獻之定位}
\label{subsec:graph_contribution}

綜合本章各節的分析，圖注意力機制的增量貢獻應定位於訊號穩定性、拓樸可解釋性與截面辨識力的提升，而非對非圖模型的全面性績效主導。DNN 在短期與中期視窗已展現具競爭力的截面排序能力，GAT 拓樸系列與 GAT 中性化系列的優勢主要體現於中、長期視窗的 ICIR 穩定性，以及使用具有經濟意義之產業圖拓樸時的截面差異保留。GAT-universe 的結果則說明，圖的引入若不搭配合理的拓樸設計，反而因過度平滑化降低個股截面辨識力。

為量化上述增量貢獻的統計顯著性，本文針對長期視窗進行 Diebold-Mariano（DM）配對比較檢定，以直接回答「圖模型的截面排序能力是否統計上優於非圖基準」的核心問題。

\textbf{Diebold-Mariano 檢定。}DM 檢定~\cite{diebold1995dm}以逐期損失差序列 $d_t = L(\hat{y}^A_t, y_t) - L(\hat{y}^B_t, y_t)$ 為基礎，虛無假說為 $H_0: \mathbb{E}[d_t] = 0$（兩模型預測能力相等）。本文採用截面排名相關係數（IC）的負值作為損失函數，即 $L = -\text{IC}_t$，$d_t = \text{IC}^B_t - \text{IC}^A_t$ 表示模型 A 每日相對 B 的 IC 超額。由於 $d_t$ 序列同樣存在序列自相關，DM 統計量的標準誤以 Newey-West HAC 估計量計算，確保推論的有效性。

結果以 DNN 為非圖基準，對長期視窗各 GAT 模型進行配對檢定，呈現明確的分層（各模型數值可對照表~\ref{tab:results_overview_master}）：
\begin{itemize}
  \item \textbf{GAT-IndNeutral 對 DNN}：平均每日 IC 差 $+0.0055$，DM 統計量 $= 2.330$（$p = 0.010$），在 5\% 顯著水準下拒絕 $H_0$。
  \item \textbf{GAT-full 對 DNN}：DM 統計量顯著（$p = 0.031$），確認中性化延伸模型均具有統計可辨識的排序能力增益。
  \item \textbf{純圖模型（GAT-industry、GAT-universe、GAT-TwoGraph）對 DNN}：DM 檢定均未達顯著水準（$p > 0.05$），儘管其 ICIR 在點估計上高於或接近 DNN。
\end{itemize}

這一分層結果具有重要的方法論意涵：圖拓樸結構的引入，若不搭配中性化機制，其所帶來的絕對 IC 提升在統計上無法與 DNN 區分。換言之，\textbf{中性化機制——而非圖連結本身——才是可統計驗證的增量來源}。此發現將「圖模型的貢獻」定位於訊號的精煉與去噪（中性化約束），而非單純的鄰域資訊聚合。

\textbf{訓練集與測試集指標對照。}表~\ref{tab:results_overview_master}「Daily IC 測試/訓練比」欄位揭示各模型泛化能力的系統性差異。短期視窗中，Linear、XGBoost 與 LSTM 的 Daily IC 測試/訓練比均僅為 0.06，為極端過擬合。隨視窗延長，比值整體提升：長期視窗中 GAT-IndNeutral（0.38）與 DNN（0.38）並列最高，在達到更高測試 Daily IC 的同時維持相近的泛化效率；XGBoost 長期比值（0.16）仍顯著偏低，其高報酬數字應合併診斷解讀。

\subsection{研究限制}
\label{subsec:limitations}

本實驗存在若干重要限制，應於解讀結果時一併考慮。

其一，回測尚未納入交易成本、市場衝擊與換手率約束，現有績效數字應視為無摩擦假設下的上限估計，實際可落地的超額報酬將低於本文所報告的數值。

其二，本文已針對長期視窗 GAT-IndNeutral 與 DNN 進行 Newey-West $t$ 檢定（$t = 11.76$，$p < 0.001$）與 Diebold-Mariano 配對比較（$p = 0.010$），以及 Block Bootstrap ICIR 信賴區間估算（95\% CI = [0.624, 0.957]）。然而，短期與中期視窗及其餘模型組合的逐一統計檢定、分年度情境分析與跨市場條件分組驗證，有待後續研究進一步補充，以更全面評估各模型在不同市場環境下的跨期一致性。

其三，本文圖結構所使用之產業分類為穩定外生資訊，不涉及前視偏誤；但若未來研究採用動態圖（如滾動相關係數）構建邊，則需嚴格確保圖建構僅使用截至當前交易日的訓練期資訊，以避免前視偏誤影響評估結果。

\FloatBarrier
